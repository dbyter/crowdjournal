# Data

This experiment requires no external datasets.

All input values are the even integers 4 through 100,000 and the set of primes up to 100,000, both generated programmatically in `code/run_experiment.py`.
